<!doctype html>
<html lang="en">
<head>
<meta charset="utf-8">
<title>ProductoParcial2</title>
<style>
  body {
    font-family: Arial, sans-serif;
    background-color: #f0f0f0;
    display: flex;
    justify-content: center;
    align-items: center;
    height: 100vh;
    margin: 0;
  }
  .container {
    background-color: #d3d3d3;
    padding: 20px;
    border-radius: 8px;
    box-shadow: 0 0 10px rgba(0, 0, 0, 0.1);
    text-align: center;
    max-width: 600px;
    width: 100%;
    overflow: hidden;
  }
  h1 {
    margin-bottom: 20px;
  }
  select, input[type="submit"], .nav-link {
    margin: 10px 0;
  }
  input[type="submit"] {
    padding: 10px 20px;
    font-size: 16px;
    color: white;
    background-color: #28a745;
    border: none;
    border-radius: 4px;
    cursor: pointer;
  }
  input[type="submit"]:hover {
    background-color: #218838;
  }
  .nav-link {
    display: inline-block;
    padding: 10px 20px;
    font-size: 16px;
    color: white;
    background-color: #007bff;
    border: none;
    border-radius: 4px;
    text-decoration: none;
  }
  .nav-link:hover {
    background-color: #0056b3;
  }
  .terminal {
    background-color: black;
    color: white;
    font-family: monospace;
    padding: 10px;
    margin-top: 20px;
    border-radius: 4px;
    box-shadow: 0 0 10px rgba(0, 0, 0, 0.5);
    max-height: 300px;
    overflow-y: scroll;
    text-align: left;
  }
</style>
</head>
<body>
<div class="container">
  <h1>Josser's</h1>
  <form id="playbookForm" action="/run_playbook" method="post">
    <label for="playbook">Selecciona el Playbook:</label><br>
    <select id="playbook" name="playbook">
      {% for playbook in playbooks %}
      <option value="{{ playbook }}">{{ playbook }}</option>
      {% endfor %}
    </select><br><br>
    <input type="submit" value="Ejecutar Playbook">
  </form>
  <a href="/playbook_factory" class="nav-link">Fábrica de Playbooks</a>
  <div id="terminalOutput" class="terminal"></div>
</div>

<script>
  const playbookForm = document.getElementById('playbookForm');
  const terminalOutput = document.getElementById('terminalOutput');

  playbookForm.addEventListener('submit', function(event) {
    event.preventDefault();

    const playbook = document.getElementById('playbook').value;

    terminalOutput.innerHTML = '';

    const xhr = new XMLHttpRequest();
    xhr.open('POST', '/run_playbook', true);
    xhr.setRequestHeader('Content-Type', 'application/x-www-form-urlencoded');

    xhr.onreadystatechange = function() {
      if (xhr.readyState === XMLHttpRequest.DONE) {
        if (xhr.status === 200) {
          const output = xhr.responseText.trim();
          simulateTerminalOutput(output);
        } else {
          terminalOutput.innerHTML += '<div>Error occurred: ' + xhr.statusText + '</div>';
        }
      }
    };

    xhr.send('playbook=' + encodeURIComponent(playbook));
  });

  function simulateTerminalOutput(output) {
    const lines = output.split('\n');
    let delay = 0;

    lines.forEach(function(line) {
      setTimeout(function() {
        terminalOutput.innerHTML += '<div>' + line + '</div>';
        terminalOutput.scrollTop = terminalOutput.scrollHeight;
      }, delay);
      delay += 100;
    });
  }
</script>
</body>
</html>